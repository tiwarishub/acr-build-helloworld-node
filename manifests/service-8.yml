apiVersion: v1
kind: Service
metadata:
    name: "petestaks-aa2c"
    labels:
        app: "petestaks-aa2c"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "petestaks-aa2c"