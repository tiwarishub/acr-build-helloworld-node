apiVersion: v1
kind: Service
metadata:
    name: "petestaks-00ab"
    labels:
        app: "petestaks-00ab"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "petestaks-00ab"