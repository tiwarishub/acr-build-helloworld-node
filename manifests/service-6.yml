apiVersion: v1
kind: Service
metadata:
    name: "petestaks-4175"
    labels:
        app: "petestaks-4175"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "petestaks-4175"