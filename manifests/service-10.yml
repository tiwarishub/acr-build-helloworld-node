apiVersion: v1
kind: Service
metadata:
    name: "petestaks-c529"
    labels:
        app: "petestaks-c529"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "petestaks-c529"