apiVersion: v1
kind: Service
metadata:
    name: "petestaks-7bf2"
    labels:
        app: "petestaks-7bf2"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "petestaks-7bf2"