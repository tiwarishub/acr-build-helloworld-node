on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    
    - uses: actions/checkout@master
    
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Create ACR
      id: create-acr
      run: |
        az acr create -n myfirstprojrct51 -g myfirstprojrct51ab56-rg --location "South India" --sku Standard --admin-enabled
        echo "::set-output name=acr_username::`az acr credential show -n myfirstprojrct51 --query username | xargs`"
        echo "::set-output name=acr_password::`az acr credential show -n myfirstprojrct51 --query passwords[0].value | xargs`"
        echo "::add-mask::`az acr credential show -n myfirstprojrct51 --query passwords[0].value | xargs`"

    - uses: azure/docker-login@v1
      with:
        login-server: myfirstprojrct51.azurecr.io
        username: ${{ steps.create-acr.outputs.acr_username }}
        password: ${{ steps.create-acr.outputs.acr_password }}

    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/" -f  "Dockerfile" -t myfirstprojrct51.azurecr.io/petestaks:${{ github.sha }}
        docker push myfirstprojrct51.azurecr.io/petestaks:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      id: login
      with:
         kubeconfig: ${{ secrets.aks_petestaks_kubeConfig }}
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep petestaks06aa | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace petestaks06aa`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: petestaks06aa
        container-registry-url: myfirstprojrct51.azurecr.io
        container-registry-username: ${{ steps.create-acr.outputs.acr_username }}
        container-registry-password: ${{ steps.create-acr.outputs.acr_password }}
        secret-name: petestaksdockerauth

    - uses: azure/k8s-bake@v1
      with:
        renderEngine: 'helm'
        helmChart: aks-helloworld
        releaseName: 'aks-helm-release'
        helm-version: 'latest' 
        silent: 'false'
        overrideFiles: |
          
        overrides: |
          replicas:2
          image.repository:myfirstprojrct51.azurecr.io/petestaks
          image.tag:${{ github.sha }}
          imagePullSecrets:
            -name:petestaksdockerauth
      id: bake

    - uses: azure/k8s-deploy@v1
      with:
        namespace: petestaks06aa
        manifests: ${{ steps.bake.outputs.manifestsBundle }}
        images: |
          myfirstprojrct51.azurecr.io/petestaks:${{ github.sha }}
        imagepullsecrets: |
          petestaksdockerauth