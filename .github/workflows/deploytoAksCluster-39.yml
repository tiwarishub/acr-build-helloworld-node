on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    
    - uses: azure/docker-login@v1
      with:
        login-server: 06cd9c503afa4455b32b81fb0da4d8ff.azurecr.io
        username: ${{ secrets.acr_06cd9c503afa4455b32b81fb0da4d8ff_username }}
        password: ${{ secrets.acr_06cd9c503afa4455b32b81fb0da4d8ff_password }}
    
    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/" -f  "Dockerfile" -t 06cd9c503afa4455b32b81fb0da4d8ff.azurecr.io/akskodey:${{ github.sha }}
        docker push 06cd9c503afa4455b32b81fb0da4d8ff.azurecr.io/akskodey:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      with:
         kubeconfig: ${{ secrets.aks_akskodey_kubeConfig }}
      id: login
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep akskodeyb58a | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace akskodeyb58a`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: akskodeyb58a
        container-registry-url: 06cd9c503afa4455b32b81fb0da4d8ff.azurecr.io
        container-registry-username: ${{ secrets.acr_06cd9c503afa4455b32b81fb0da4d8ff_username }}
        container-registry-password: ${{ secrets.acr_06cd9c503afa4455b32b81fb0da4d8ff_password }}
        secret-name: akskodeydockerauth
       
    - uses: azure/k8s-bake@v1
      with:
        renderEngine: 'helm'
        helmChart: aks-helloworld
        releaseName: 'aks-helm-release'
        helm-version: 'latest' 
        silent: 'false'
        overrideFiles: |
          
        overrides: |
          replicas:2
          image.repository:06cd9c503afa4455b32b81fb0da4d8ff.azurecr.io/akskodey
          image.tag:${{ github.sha }}
          imagePullSecrets:
            -name:akskodeydockerauth
      id: bake

    - uses: azure/k8s-deploy@v1.2
      with:
        namespace: akskodeyb58a
        manifests: ${{ steps.bake.outputs.manifestsBundle }}
        images: |
          06cd9c503afa4455b32b81fb0da4d8ff.azurecr.io/akskodey:${{ github.sha }}
        imagepullsecrets: |
          akskodeydockerauth